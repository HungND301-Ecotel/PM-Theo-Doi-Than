services:
  coal-tracking-reverse-proxy-service:
    build:
      context: ./reverse_proxy
    image: ${REGISTRY}/pmtdt-reverse-proxy-img:${VERSION}
  coal-tracking-frontend-service:
    build:
      context: ./frontend
    image: ${REGISTRY}/pmtdt-frontend-img:${VERSION}

  coal-tracking-backend_service:
    build:
      context: ./backend
      dockerfile: master-data-serivce/Dockerfile
    image: ${REGISTRY}/pmtdt-backend-img:${VERSION}
